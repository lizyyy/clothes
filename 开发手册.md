# Clothes iOS 前端开发手册

## 1. 适用范围
- 目录：`clothes-ios/` 与其下所有 Swift 源码、测试与扩展代码。
- 目标：保证客户端改动稳定、可读、可测试。
- 交付铁律：代码改动完成后，必须同步更新文件头注释与所属目录 `ARCH.md`。

## 2. 目录职责
- `Clothes/`：主应用业务实现。
- `ClothesUITests/`：UI 自动化测试与页面对象。
- `ShareExtension/`、`ClothesShareExtension/`：系统分享扩展。
- `AppStore/`：上架说明、隐私政策与素材规范。

## 3. 质量红线（强制）
- 文件行数：`<= 800`（新文件强制；历史超限文件禁止继续膨胀）。
- 函数行数：`<= 30`。
- 嵌套深度：`<= 3` 层。
- 分支数量：`<= 3`（`if/else-if/for/while/switch` 合计）。
- 自动化评分铁律：代码会被程序化测试打分，必须覆盖边缘用例（空值、极值、异常输入、并发与超时），禁止只满足主流程。
- UITest 同步铁律：凡业务逻辑或交互流程改动（尤其登录/注册/支付/状态切换），必须在同一提交同步更新 `ClothesUITests`，禁止功能已改但 UI 用例仍指向旧流程。

## 4. 工程约束
- 每个 Swift 文件开头必须保留 4 行注释：`input`、`output`、`pos`、更新联动声明。
- 任何功能/架构/写法变化，必须同步更新对应目录 `ARCH.md`。
- UI 代码禁止在超长函数中继续堆逻辑；优先拆分子 View、ViewModel、Service。

## 5. 开发流程
1. 拉取最新代码并确认依赖。
2. 完成功能后补测试（单测/UI 测试按需求选择）。
3. 本地构建校验：
   - `xcodebuild -project Clothes.xcodeproj -scheme Clothes -destination 'platform=iOS Simulator,name=iPhone 17' build`
4. 提交前由 hook 自动执行规范检查（质量红线 + 头注释）。

## 6. Hook 与脚本
- 质量检查脚本：`scripts/check_project_rules.py`
- pre-commit：`.githooks/pre-commit`
- 安装命令：`bash scripts/install_hooks.sh`

## 7. 历史遗留处理策略
- 历史超限文件可分阶段治理，但本次触达函数必须满足红线。
- 若需求必须改动超限函数，先切分成小函数再新增逻辑。
- 新增业务优先放入新模块，避免继续扩大巨型文件。
